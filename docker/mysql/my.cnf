[mysqld]
character-set-server = utf8mb4
collation-server     = utf8mb4_unicode_ci
default-time-zone    = 'Asia/Tokyo'

# Prepared statement キャッシュ対策
table_definition_cache = 400
table_open_cache       = 400
